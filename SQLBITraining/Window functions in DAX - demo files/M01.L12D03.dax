DEFINE
    VAR Test =
        SELECTCOLUMNS ( { 1, 2, 3 }, "Number", [Value] )

EVALUATE
GENERATE (
    Test,
    SELECTCOLUMNS (
        INDEX ( 1, Test, ORDERBY ( [Number] ), MATCHBY ( [Number] ) ),
        "First", [Number]
    )
)
